---

# A very simple fixture used to prove the basic mechanics of the ansible
# testing framework.

- hosts: testcasehost
  gather_facts: no

  tasks:

    - name: Print hello world
      debug: msg="Hello, World!"

