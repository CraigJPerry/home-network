---

- name: hardware | Install Arduino
  yum: state=present pkg={{ item }}
  with_items:
    - arduino
    - arduino-doc

- name: hardware | Permit users access to arduino devices
  user: name={{ item }} append=yes groups="lock,dialout"
  with_items: arduino_users

